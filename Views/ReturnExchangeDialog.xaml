<Window x:Class="MyPOS99.Views.ReturnExchangeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Return / Exchange Items" 
        Width="700" Height="750"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- Match SalesWindow button styles -->
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="25,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDC3C7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#27AE60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#95A5A6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7F8C8D"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#9B59B6" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Return / Exchange Items" FontSize="22" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Scan product codes of items to return. Stock will be updated and credit applied." 
                          FontSize="12" Foreground="White" Opacity="0.9" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Product Code Input -->
                <TextBlock Grid.Row="0" Text="Scan barcode or search by code/name:" 
                          FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>

                <TextBox Grid.Row="1" x:Name="ProductCodeTextBox" 
                        FontSize="18" Padding="15" Margin="0,0,0,15"
                        KeyDown="ProductCodeTextBox_KeyDown"
                        ToolTip="Scan barcode or type product code/name and press Enter"/>

                <!-- Return Items List -->
                <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="2" CornerRadius="6" MinHeight="200">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#F8F9FA" Padding="12" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                            <TextBlock Text="Items to Return:" FontWeight="Bold" FontSize="14"/>
                        </Border>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ListBox Grid.Row="1" x:Name="ReturnItemsListBox" Background="Transparent" BorderThickness="0" Padding="5">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#ECF0F1" BorderThickness="0,0,0,1" Padding="10,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" FontSize="13"/>
                                                <TextBlock Text="{Binding ProductCode}" FontSize="11" Foreground="#7F8C8D"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="1" Text="{Binding Quantity, StringFormat='Qty: {0}'}" 
                                                      Margin="15,0" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>

                                            <TextBlock Grid.Column="2" Text="{Binding Price, StringFormat='Rs. {0:N2}'}" 
                                                      Margin="15,0" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>

                                            <Button Grid.Column="3" Content="?" Click="RemoveItemButton_Click"
                                                   Background="#E74C3C" Foreground="White"
                                                   Width="32" Height="32" FontSize="14" FontWeight="Bold"
                                                   BorderThickness="0" Cursor="Hand" Margin="10,0,0,0">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}" 
                                                                           CornerRadius="4">
                                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                                        VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#C0392B"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        </ScrollViewer></Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Return Type Selection -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Return Type:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="ExchangeRadioButton" Content="Exchange (Add new items)" 
                                IsChecked="True" FontSize="13" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <RadioButton x:Name="CashReturnRadioButton" Content="Cash Return" 
                                FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Return Discount Field (only for cash returns) -->
                <StackPanel x:Name="ReturnDiscountPanel" Visibility="Collapsed" Margin="0,10,0,0">
                    <TextBlock Text="Original Discount Given (if any):" FontSize="13" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                    <TextBox x:Name="ReturnDiscountTextBox" FontSize="14" Padding="10" 
                            ToolTip="Enter the discount amount that was given in original sale"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Total -->
        <Border Grid.Row="3" Background="#E8F5E9" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <TextBlock Text="Return Amount:" FontSize="16" FontWeight="SemiBold" Foreground="#27AE60"/>
                <TextBlock x:Name="TotalAmountText" Text="Rs. 0.00" 
                          FontSize="20" FontWeight="Bold" Foreground="#27AE60" 
                          HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Cancel" Click="CancelButton_Click"
                    Style="{StaticResource DangerButton}"/>

            <Button Grid.Column="2" Content="Continue" Click="ContinueButton_Click"
                    Style="{StaticResource SuccessButton}"/>
        </Grid>
    </Grid>
</Window>


